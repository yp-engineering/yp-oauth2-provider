# -*- ruby -*-

require "rubygems"
require "hoe"

Hoe.plugin :isolate, :version, :git

Hoe.spec "yp-oauth2-provider" do
  developer("Brian Henderson", "bhenderson@yp.com")

  self.testlib = :none

  require_ruby_version '~> 2.0'

  license "MIT" # this should match the license in the README
end

# vim: syntax=ruby
